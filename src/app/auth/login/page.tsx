import { LoginForm } from '@/features/auth/components/LoginForm';

export const metadata = {
  title: 'Sign In | CanvasCollect',
  description: 'Sign in to your CanvasCollect account',
};

export default function LoginPage() {
  return <LoginForm />;
}
