# IMP-032 — Optional product-surface semantics

<objective>
Apply DEC-011 through DEC-013 so embed, meeting timer, and AR behavior match
what the product can safely support.
</objective>

<steps>
1. Embeds: implement the selected sandboxed allowlist with CSP/permission tests,
   or present them consistently as link previews with no live execution.
2. Timer: label/store it as personal UI, or add server-authoritative synchronized
   state with reconnect and multi-client tests.
3. AR: if supported, test camera permission, denial, reopen, backgrounding,
   fallback, and stream release on the selected real-device/browser matrix. If
   experimental/off, gate and label it accordingly.
4. Update user-facing copy and help to promise only the selected behavior.
</steps>

<acceptance>
- No optional surface implies synchronization, execution, or device support it
  does not implement.
</acceptance>

<verification>
Selected feature tests; real-device AR matrix when enabled; type/lint/build.
</verification>
